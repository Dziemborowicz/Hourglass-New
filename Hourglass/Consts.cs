using System;

using Hourglass.Properties;

namespace Hourglass;

internal static class Consts
{
    public static readonly Uri FAQUri   = new(Resources.FAQUrl);
    public static readonly Uri UsageUri = new(Resources.UsageUrl);
}
